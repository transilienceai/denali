# P0 closeout checkpoint — 2026-09-17

This checkpoint records the last independently actionable P0 work and the exact external gates
that remain. It contains bounded organization, connection, role, and deployment identifiers only;
it excludes credentials, tokens, callback codes, provider payloads, prompts, and responses.

## Completed in this checkpoint

- Removed the temporary explicit `neondb_owner` CRUD compatibility grant from all 33 application
  tables after the rotated production runtime had been activated.
- Verified zero owner CRUD grants remain, `denali_runtime` retains read access to all 33 tables,
  and `denali_migration` remains their owner.
- Invoked the currently deployed production `active_connection_status` function after the revoke;
  it returned 21 rows spanning AWS, Azure, GCP, Entra, Google Workspace, GitHub, and Azure Repos.
- Re-ran the production GitHub source collection. All five exact repositories completed source,
  inventory, and posture collection, using 463 of the 4,000-request bound with no failed
  repository and no request-budget exhaustion.
- Preserved the truthful partial code-to-cloud result for `kkmookhey/shasta`: one Terraform
  declaration computes its function name instead of resolving the provider region, single allowed
  account, and function name to literals. This is a deterministic correlation coverage gap, not a
  GitHub credential, validation, or source-collection failure.
- Reconfirmed that the retained disabled `AWS AgentCore P0 Acceptance` connection and its evidence
  remain intact, as the operator requested.

## External gates requiring owner or operator action

1. **Neon managed operations.** The signed-in Neon account has an organization but zero projects.
   A production-project owner must invite it or provide a scoped Neon API credential before Denali
   can enable and exercise alerts, verify backup/PITR settings, create a restore branch, and run the
   restore drill.
2. **Vercel runtime monitoring.** Privacy-safe Web Analytics code is deployed, and protected
   production/deployment checks are passing. Vercel rejects GitHub login because the GitHub
   identity is not linked to the existing account. The account owner must log in using its current
   email or passkey method, link GitHub, enable Web Analytics for the project, and permit the
   runtime exercise.
3. **Clerk member matrix.** The isolation Organization has one member and the existing Organization
   has three; all four memberships are admins. Finishing the hosted `org:member` matrix requires
   authorization to temporarily downgrade and restore the current user in the isolation
   Organization, or authorization to create/invite a bounded test member.
4. **Provider retirement records.** The strict provider exit contract still lacks complete dated
   create -> setup/callback -> validate -> collect -> disable -> delete records for Azure, GCP,
   Entra, Google Workspace, and GitHub, plus a dedicated preservation record for the reported Azure
   Repos pass. The retained AgentCore fixture is intentionally not the AWS delete specimen; use a
   separate disposable AWS connection for delete and post-delete retention proof.

P0 must not be described as complete until these gates are either exercised and evidenced or
explicitly accepted as launch blockers by the responsible owner. P1 implementation may begin only
under the roadmap's existing rule that every external gate is evidenced or recorded as an explicit
blocker; doing so does not convert an open P0 control into a pass.
